const initialState = {
    credentials: {
        username:'',
        password:''
    },
    error: ''
}
export const appReducer =(state = initialState, action) =>{
    switch (action.type){
        default: return state}
        }